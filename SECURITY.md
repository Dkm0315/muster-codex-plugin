# Security

Report vulnerabilities privately to the repository owner through GitHub security advisories.

The full-file tool restricts access to regular text files under the supplied active workspace, rejects common secret/key filenames, caps file size, and never writes project files. Plugin state is stored under the Codex state directory.
